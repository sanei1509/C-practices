#include "binary_trees.h"

/**
* binary_tree_delete - Borrar un arbol binario enteramente
* @tree: Puntero al root del arbol binario a borrar
* Si el arbol es NULL, NO HACE NADA
*/

void binary_tree_delete(binary_tree_t *tree)
{
    // si el arbol es NULL no hago nada
    if (tree == NULL)
        return;
}
